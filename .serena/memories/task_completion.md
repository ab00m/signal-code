# Task Completion
- For Git/Godot housekeeping changes, review `.gitignore` and `.gitattributes` content before finishing.
- If LFS rules change after files are already tracked, remind the user to run `git lfs migrate import` or re-add affected files as appropriate.
- For gameplay/code changes, validate in Godot editor when possible and run the project from the configured main scene.