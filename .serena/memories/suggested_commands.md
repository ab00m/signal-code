# Suggested Commands
- Open/edit project with Godot 4.6.
- Run from the editor using main scene `scenes/opening/opening.tscn`.
- Git basics on Windows PowerShell: `git status`, `git diff`, `git add`, `git commit`.
- LFS setup before pushing large assets: `git lfs install`, then ensure `.gitattributes` contains the needed LFS patterns.